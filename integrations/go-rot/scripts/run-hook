#!/bin/sh
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

if [ -n "$GO_ROT_NODE" ] && [ -x "$GO_ROT_NODE" ]; then
  exec "$GO_ROT_NODE" "$SCRIPT_DIR/hook-client.mjs"
fi

if [ -n "$CODEX_CLI_PATH" ]; then
  CODEX_RESOURCES="$(CDPATH= cd -- "$(dirname -- "$CODEX_CLI_PATH")" && pwd)"
  CODEX_NODE="$CODEX_RESOURCES/cua_node/bin/node"
  if [ -x "$CODEX_NODE" ]; then
    exec "$CODEX_NODE" "$SCRIPT_DIR/hook-client.mjs"
  fi
fi

for GO_ROT_NODE_CANDIDATE in \
  /opt/homebrew/bin/node \
  /usr/local/bin/node \
  /usr/bin/node
do
  if [ -x "$GO_ROT_NODE_CANDIDATE" ]; then
    exec "$GO_ROT_NODE_CANDIDATE" "$SCRIPT_DIR/hook-client.mjs"
  fi
done

exec /usr/bin/env node "$SCRIPT_DIR/hook-client.mjs"
